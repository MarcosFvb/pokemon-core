package core.pokemon.server;

import core.pokemon.IApplicationDist;
import core.pokemon.server.dedicated.PokemonServer;

@SuppressWarnings("all")
public class ServerDist implements IApplicationDist {

    private PokemonServer pokemonServer;
}
